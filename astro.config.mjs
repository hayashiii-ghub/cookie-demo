import { defineConfig } from 'astro/config';

// Single-page scroll reel. Static output (default) — Vercel auto-detects Astro
// and serves the built `dist/`. No adapter needed (no SSR).
export default defineConfig({});
